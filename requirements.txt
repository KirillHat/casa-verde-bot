# --- Web framework ----------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.32.0

# --- WhatsApp via Twilio ----------------------------------------------------
twilio==9.3.7

# --- Anthropic Claude -------------------------------------------------------
anthropic==0.39.0

# --- Google Sheets CRM ------------------------------------------------------
gspread==6.1.4
google-auth==2.35.0

# --- Slack notifications ----------------------------------------------------
# (we just POST JSON to an incoming webhook via httpx, no SDK needed)

# --- Database ---------------------------------------------------------------
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0

# --- Config & runtime utils -------------------------------------------------
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.0.1
httpx==0.27.2
python-multipart==0.0.12

# --- Observability ----------------------------------------------------------
structlog==24.4.0
sentry-sdk[fastapi]==2.17.0
